export default function App({ selectedId, isEditing, searchText }) {
  return (
    <div className="main">
      <div
        style={{ backgroundColor: 'red', width: '400px', height: '300px' }}
      ></div>
    </div>
  )
}
